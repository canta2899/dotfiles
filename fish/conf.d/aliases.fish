# Aliases (fish's `alias` creates tiny wrapper functions)
alias icloud='cd $CLOUDDOCS'

alias ztc='zerotier-cli'

alias ls='logo-ls -Dh'

alias we='du -d 0 -h'

alias tn='tmux new-session -t'
alias tl='tmux ls'
alias t='tmux'
alias ta='tmux attach -t'

alias vim='nvim'
alias v='nvim'

alias nc='netcat'

alias ii='open'

alias gdu="$HOME/go/bin/gdu"

alias sed='gsed'
alias date='gdate'

alias flushdnscache='sudo dscacheutil -flushcache; and sudo killall -HUP mDNSResponder'
